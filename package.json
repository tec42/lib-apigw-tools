{
  "name": "@ivodenwag/lib-apigw-tools",
  "version": "1.4.0",
  "description": "Bundle OpenAPI specs for AWS API Gateway import — shared library for tec42 backend services",
  "type": "module",
  "bin": {
    "lib-apigw-tools": "./bundle-apigw-spec.mjs",
    "tec42-bundle-apigw-spec": "./bundle-apigw-spec.mjs"
  },
  "scripts": {
    "test": "docker run --rm -v \"$PWD:/workspace\" -w /workspace node:20-alpine node --test bundle-apigw-spec.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@redocly/cli": ">=1.0.0"
  },
  "keywords": [
    "aws",
    "api-gateway",
    "openapi",
    "terraform",
    "tec42"
  ],
  "license": "MIT"
}
